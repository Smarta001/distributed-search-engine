# NOTE: these are the same dev credentials already in your docker-compose.yml.
# Do not commit real production secrets here — use kubectl create secret
# or a secrets manager for anything beyond local/class-project use.
apiVersion: v1
kind: Secret
metadata:
  name: search-engine-secrets
  namespace: search-engine
type: Opaque
stringData:
  POSTGRES_USER: search
  POSTGRES_PASSWORD: search123
  RABBITMQ_USER: guest
  RABBITMQ_PASSWORD: guest
